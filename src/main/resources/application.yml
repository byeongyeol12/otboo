spring:
  application:
    name: otboo

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true        # 실행되는 쿼리 콘솔에 출력
    properties:
      hibernate:
        format_sql: true  # 쿼리 예쁘게 보기 좋게
